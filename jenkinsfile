pipeline {
    agent any

    environment {
        DOCKERHUB_CRED = credentials('dockerhub')
        IMAGE_NAME = "vivek5041/node_app"
    }

    stages {

        stage('Checkout') {
            steps {
                git url: 'https://github.com/1ms24mc111/node_app.git',
                    branch: 'master'
            }
        }

        stage('Build Docker Image') {
            steps {
                script {
                    dockerImage = docker.build("${IMAGE_NAME}:latest")
                }
            }
        }

        stage('Push to DockerHub') {
            steps {
                script {
                    docker.withRegistry(
                        'https://index.docker.io/v1/',
                        'dockerhub'
                    ) {
                        dockerImage.push('latest')
                    }
                }
            }
        }
    }

    post {
        success {
            echo " Node App Pipeline Successful"
        }
        failure {
            echo " Node App Pipeline Failed"
        }
        always {
            echo " Cleaning Workspace"
            deleteDir()
        }
    }
}
